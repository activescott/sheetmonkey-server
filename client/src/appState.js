'use strict'
// redux default app state:
const appState = {
  /* Could offer an initial state here, but the reducers each have an initial state they'll use
     too. I'm using their initial state instead.
  user: { loggedIn: false },
  name: 'initial world',
  plugins: []
  */
}

export default appState
